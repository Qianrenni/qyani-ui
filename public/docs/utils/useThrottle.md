# 函数 API

## `useThrottle`

```ts
export const useThrottle = <T extends (...args: any[]) => any>(
  func: T,
  interval: number = 16
): ((...args: Parameters<T>) => void) => { ... }
```

### 参数

| 参数      | 类型                           | 默认值  | 必填 | 说明                     |
|---------|------------------------------|------|----|------------------------|
| `func`  | `(...args: any[]) => any`    | —    | 是  | 需要节流的原函数              |
| `interval` | `number`                    | `16` | 否  | 节流间隔时间（毫秒），默认约 60fps |

### 返回值

- 类型：`(...args: Parameters<T>) => void`
- 说明：节流后的函数包装器，调用时会在指定间隔内最多执行一次原函数

### 行为说明

- 使用 `Date.now()` 记录时间戳判断是否达到节流间隔
- 只有距离上次执行超过 `interval` 毫秒时，才会触发原函数执行
- 保留原函数的 `this` 上下文和参数传递
- 典型用于高频事件（如 scroll、resize、input）的性能优化

> 💡 默认间隔为 16ms（接近 60 FPS），适合动画与交互响应的流畅性控制